@model IEnumerable<ArtGallery.Models.ArtworkView>

@{
    ViewData["Title"] = "Artworks";
    Layout = "_Layout";
}

<h2>Artworks</h2>

<p>
    <a href="@Url.Action("Create")" class="btn btn-primary">Create New</a>
</p>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Title</th>
            <th>Artist</th>
            <th>Description</th>
            <th>Category</th>
            <th>Status</th>
            <th>Price</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Title</td>
                <td>@item.ArtistName</td>
                <td>@item.Description</td>
                <td>@item.Category</td>
                <td>@item.Status</td>
                <td>@item.Price</td>
                <td>
                    <a href="@Url.Action("Edit", new { id = item.ArtworkId })" class="btn btn-warning">
                        <i class="fas fa-edit"></i>
                    </a>
                    <a href="@Url.Action("Details", new { id = item.ArtworkId })" class="btn btn-info">
                        <i class="fas fa-info-circle"></i>
                    </a>
                    <a href="@Url.Action("Delete", new { id = item.ArtworkId })" class="btn btn-danger">
                        <i class="fas fa-trash"></i>
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>